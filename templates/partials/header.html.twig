<nav class="navbar bg-primary navbar-expand-lg" data-bs-theme="dark">
  <div class="container-fluid  bg-primary text-white">
    <a class="navbar-brand" href="{{ url('home') }}">Mon site</a>
    <div class="collapse navbar-collapse"">
      <ul class="navbar-nav">
      {#}
        <li class="nav-item">
          <a class="nav-link {{ app.current_route == 'home' ? 'active' : '' }}" href="{{ url('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.current_route starts with 'fauna.' ? 'active' : '' }}" href="{{ url('fauna.index') }}">La faune</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ app.current_route starts with 'flora.' ? 'active' : '' }}" href="{{ url('flora.index') }}">La flore</a>
        </li>
        #}
  </div>
</nav>